{
  "lookup": {
    "PROCX": {
      "template": "=PROCX({search_value};{search_range};{return_range};\"Não encontrado\")",
      "explanation": "Procura um valor em um intervalo e retorna a informação correspondente."
    },
    "PROCV": {
      "template": "=PROCV({search_value};{table_range};{column_index};FALSO)",
      "explanation": "Procura um valor na primeira coluna de um intervalo e retorna dados de outra coluna."
    }
  },
  "sum": {
    "SOMA": {
      "template": "=SOMA({range})",
      "explanation": "Soma todos os valores de um intervalo."
    },
    "SOMASE": {
      "template": "=SOMASE({criteria_range};{criteria};{sum_range})",
      "explanation": "Soma valores que atendem a um critério."
    },
    "SOMASES": {
      "template": "=SOMASES({sum_range};{criteria_range1};{criteria1};{criteria_range2};{criteria2})",
      "explanation": "Soma valores que atendem simultaneamente a múltiplos critérios."
    }
  },
  "count": {
    "CONT.VALORES": {
      "template": "=CONT.VALORES({range})",
      "explanation": "Conta quantas células preenchidas existem em um intervalo."
    },
    "CONT.SE": {
      "template": "=CONT.SE({range};{criteria})",
      "explanation": "Conta células que atendem a um critério."
    },
    "CONT.SES": {
      "template": "=CONT.SES({criteria_range1};{criteria1};{criteria_range2};{criteria2})",
      "explanation": "Conta células ou registros que atendem simultaneamente a múltiplos critérios."
    }
  },
  "condition": {
    "SE": {
      "template": "=SE({condition};{true_result};{false_result})",
      "explanation": "Retorna um resultado quando uma condição é verdadeira e outro quando é falsa."
    },
    "SES": {
      "template": "=SES({condition1};{result1};{condition2};{result2};VERDADEIRO;{default_result})",
      "explanation": "Avalia múltiplas condições em sequência e retorna o resultado correspondente à primeira condição verdadeira."
    },
    "E": {
      "template": "=E({condition1};{condition2})",
      "explanation": "Retorna VERDADEIRO somente quando todas as condições forem verdadeiras."
    },
    "OU": {
      "template": "=OU({condition1};{condition2})",
      "explanation": "Retorna VERDADEIRO quando pelo menos uma condição for verdadeira."
    }
  }
}
